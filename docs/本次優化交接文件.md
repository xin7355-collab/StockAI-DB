# 🗂️ 本次優化交接文件

一次 session 內完成的全部改動、驗證結果與上線待辦。給你或後續接手者快速掌握。

---

## 一、做了哪些事(依序 10 個 commit)

| Commit | 主題 | 影響檔案 |
|---|---|---|
| `7e88747` | 技術指標 NaN 防呆(MA/MACD/布林非有限值過濾) | miner, radar_miner, momentum, potential, backtest |
| `81239a2` | api.py `/api/tech_ai` MA 計算防呆(修崩潰 + 假 MA20) | api.py |
| `f6f4b77` | 錯誤處理健全化:隔離獨立採礦步驟(一步失敗不拖垮後續) | miner, universal_radar |
| `afa21af` | DB 批次 upsert + 抽出共用模組 `common.py` | miner, common(新), 5 檔改用 |
| `84da78e` | CI:`common.py` 加進觸發 paths | daily_miner.yml, cancel-stale-miners.yml |
| `d7e2cd1` | 新增 `strategy_sim.py` 核心 + 全市場旗艦訊號 + 朱式出場回測(建議1-5) | strategy_sim(新), backtest, radar_miner, worker.js, docs |
| `406b432` | 補完建議2、3:橫盤突破/背離/量能/葛蘭碧全市場 + paper_trade 朱式出場 | strategy_sim, radar_miner, paper_trade |
| `0819928` | 三均線分批出場(第8-5章):全出 vs 分批3張 回測對照 | strategy_sim, backtest |
| `81dc6cc` | 前端:朱家泓子面板新增 4 個全市場訊號 tab | index.html |
| `b19d429` | 修正 `bang_stage` 末升段誤判(真實資料驗證發現) | strategy_sim |

---

## 二、新增的架構(單一真相來源)

### `common.py` — 泛用工具
`is_finite_num`(NaN/±Inf/bool 防呆)、`avg`、`safe_ma`、`load_json`、`TPE`。
只依賴標準函式庫;miner/radar/backtest/momentum/potential 皆 import。

### `strategy_sim.py` — 朱家泓策略核心(純函式,對照逐字稿逐條實作)
| 函式 | 用途 | 逐字稿 |
|---|---|---|
| `find_swings` / `trend_structure` | 轉折波高低點(±8 fractal)+ 頭頭高底底高 | 第1章 |
| `bang_stage` | 波段階段(base/main/mid/end),末升段排除 | 5-1/5-6 |
| `chu_long_entry` | 回後買上漲(旗艦)+ 上方空間≥10% gate + 分級 | 6-2 |
| `simulate_chu_exit` | 朱式動態出場(跌破5MA/破進場K低-5%/+7%鎖利/+20%加速) | 7-9 |
| `simulate_three_ma_scaled_exit` | 三均線分批3張(5/10/20MA各1/3) | 8-5 |
| `backtest_chu_swing(exit_fn=…)` | 進場+出場逐根回測期望值(可換出場模型) | — |
| `chu_eliminate` | 淘汰選股13條紅旗 | 5-3 |
| `granville` | 葛蘭碧八大買賣點 | 3-6 |
| `box_breakout` | K線橫盤突破 | 2-3/6-3 |
| `volume_signals` | 攻擊/出貨/止跌/換手/強反彈量 | 4章 |
| `kd_series`/`dif_series`/`divergence` | KD/MACD 序列 + 頂底背離 | 5章 |

### 接線點(全部加法式,不改寫既有邏輯)
- **backtest.py**:每檔跑 `backtest_chu_swing`(全出)+ 分批 → 輸出 `chu_swing_backtest` / `chu_swing_scaled_backtest`。
- **radar_miner.py**:新增全市場榜 `chu_entry / chu_granville / chu_box / chu_diverge`(寫進 `radar_matrix.json`)。
- **paper_trade.py**:回後買上漲用 `simulate_chu_exit` 逐日追蹤真實出場 → `chu_swing_summary`。
- **worker.js**:Telegram 停損對齊 −5%/+10%。
- **index.html**:朱家泓子面板新增 4 個 tab 顯示上述全市場榜(`_renderChuSignalList`)。

---

## 三、驗證結果(真實資料 2334 檔,非合成)

從 `origin/data` 抓下 2334 檔真實個股,本機複製 CI 流程實測:

- **所有新函式 × 真實股票:零崩潰**;訊號命中數合理。
- **radar_miner 完整跑(1517 檔,exit 0)**:回後買 17 / 葛蘭碧 30 / 橫盤 6 / 背離 30,`radar_matrix.json` 4 個新 key 都在、樣本正確。
- **backtest 端到端(exit 0)**:全出 vs 分批對照 ↓

| 出場 | 有訊號 | 正EV% | 交易數 | 平均期望值 | 勝率 |
|---|---|---|---|---|---|
| 全出 | 1717 | 39.3% | 18,162 | −0.22% | 26.8% |
| **分批3張** | 1607 | **41.5%** | 14,371 | **−0.08%** | 27.3% |

**判讀**:
- **分批3張略優於全出**(印證「長線保護短線」)。
- 全市場「原始訊號」平均微負(扣成本後)、勝率 ~27% → 非 bug,是「順勢緊出場」特徵(多數小賠、少數大賺);**edge 在選出正EV 的 ~40% 子集 + 過濾網**,不是無腦全買。
- 真實資料驗證抓到並修掉 `bang_stage` 末升段誤判(2006/6538 好訊號救回)。

---

## 四、上線待辦(TODO)

1. **CI 跑一次讓前端更新**:GitHub Actions → `daily_miner`(或 probe),讓正式 `radar_matrix.json` 更新,使用者才看得到 4 個新 tab。程式已在本機用真實資料驗證通過。
2. **累積 paper_trade 實戰數據**:`chu_swing_summary` 需跑幾週才有樣本,之後看 reality gap(歷史回測 vs 實戰)。
3. **門檻微調**:依真實 backtest 數字調 `chu_long_entry` 的上方空間門檻、`bang_stage` 分段界線、`chu_eliminate` 各條寬嚴。
4. **前端單一真相遷移(大部分已完成)**:見下方第六節。剩「旗艦分級邏輯完整合併」與「另 2 處 fractal 變體」屬設計取捨/低價值,未做。

---

## 六、前端單一真相遷移(index.html)

新增內嵌模組 `window.StrategySim`(mirror `strategy_sim.py`),偵測器改為「呼叫模組算邏輯 + 前端只組 UI 訊息」。每個都用 2334 檔真實資料做 **JS↔Python parity 驗證**:

| 偵測器 | 對應函式 | parity |
|---|---|---|
| `_detectBoxBreakout` | `boxBreakout` | 103/103 = 100% |
| `_detectVolumeSignals` | `volumeSignals` | 850/850 = 100% |
| `_detectGranville` | `granville`(20MA→5天斜率) | 338/340 = 99.4%(2 浮點邊界) |
| `_detectIndicatorDivergence` | `divergence`(自算KD/DIF) | 364/364 = 100% |
| `_detectChuLongEntry` 地基 | `trendStructure` | 2334/2334 = 100% |

**斜率準則(採納)**:短線飆股(5MA)→3天、波段(20/60MA)→5天。granville 與回後買上漲的月線上彎都用 5 天。

**依逐字稿補鐵則**:回後買上漲(6-2)補「收>20MA 且 20MA 上彎(5天斜率)」為高勝率鐵則,前端 `_detectChuLongEntry` 與 Python `chu_long_entry` 同步套用。

**未完成**:
- 旗艦 `chuLongEntry` 分級邏輯的完整單一真相合併(前端做頭守門 vs Python upside_room/bang_stage/淘汰)——屬 per-stock UX vs 全市場 screener 的刻意分工,轉折波地基已 100% 單一真相、逐字稿核心鐵則已一致。
- `_renderChuFourGates` / `_detectChuTrend` 的 ±8 fractal(參數變體 SW=6/70、回測用 `i` 結尾,無法乾淨套 `trendStructure`)。

---

## 五、注意事項

- **本機 `data/` 的 2334 檔**是從 `data` 分支抓來做驗證用,`.gitignore` 已忽略(不會進 repo);`radar_matrix.json`/`signal_history.json` 為本機產出,同樣忽略。
- **保留未動的安全邊界**:`api.py`(線上後端)保留自足 inline finite 檢查(避免部署包缺 common.py);`run()` 每檔 commit 的超時韌性設計不動。
- **CI 觸發 paths** 已含 `common.py`、`strategy_sim.py`(改這兩檔會觸發 daily_miner 重跑驗證)。
